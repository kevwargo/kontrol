## Rules

1. match multiple classes (or regexp) in a single rule

## Snippets: strip whitespace

```
<id>:
  snippet:
    cmd: ...
    whitespace:
      strip: bool
      strip-head: bool
      strip-tail: bool
      chars: (default ' \n\r\t')
```
